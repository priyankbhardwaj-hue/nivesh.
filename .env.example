# Optional: Use local backend API base URL during development.
# VITE_API_BASE_URL=http://127.0.0.1:8000/

# Optional: Override Lead Partner API URL if you get 404 from the default endpoint.
# Uncomment and set the correct URL from your backend (e.g. if the path uses lowercase /api/).
# VITE_LEAD_PARTNER_API_URL=https://api.nivesh.com/API/WebAddLeadPartner
